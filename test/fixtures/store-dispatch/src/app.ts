import { Component } from '@angular/core';
import { FilterComponent } from './filter';

@Component({ selector: 'app-root', imports: [FilterComponent], template: '<app-filter></app-filter>' })
export class AppComponent {}
