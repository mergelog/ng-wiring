import { Component } from '@angular/core';

/** The template file this names does not exist, so the component has no indexed template. */
@Component({ selector: 'app-missing-template', templateUrl: './missing.html' })
export class MissingTemplateComponent {}
