# 既定出力をサンプルの追跡 Map にする作業プラン

作成日: 2026-09-25
原点: `x-issue-sample-ngwi.md`

## 1. 完成形

`x-issue-sample-ngwi.md` の追跡 Map を、この作業の出力仕様とする。`data-id=nameField` の対象候補では、root から `POST ${basePath}/tasks.update` まで、サンプルの23ステップを同じ順で追えることを目標にする。途中の単なる `div` は表示しない。

既定の本文構成はサンプルどおり、タイトル、`## nameField の 遷移`、23行の Map、`## 凡例`、`## selector path`、`## exec command` とする。`selector path` は `--selector` が指定された場合に出す。status、候補の説明、分岐件数など、サンプルにない情報を既定出力へ追加しない。必要な調査情報は `--detail` に置く。

サンプルの `解析信頼度: 98%` は表示例であり、ユーザー確認により省略する。根拠のない百分率は出力しない。

| 指定 | 出力 |
| --- | --- |
| 指定なし | サンプルの短い追跡 Map |
| `--detail` | 現在の詳細 Markdown |
| `--json` | 現在の JSON |
| `--belowData` | 短い Map の `:D` データ受け渡し以降だけ |

`--detail` と `--json` は同時指定不可。`--belowData` は短い Map にだけ適用する。既定出力の変更以外に、詳細 Markdown や JSON の表示形式を変更しない。

## 2. 行の契約

サンプルの `- 23. [▶️:B:V](...) ...:行` から `- 01. [▶️:S:A](...) ...:行` までの書式、順序、種類、関係を踏襲する。`▶️` に新しい確度の意味を付けず、`❓`／`⛔` など新しい記号も追加しない。種別と関係の凡例はサンプルにあるものをそのまま使う。

リンクは実際の根拠ファイルと行を指す。サンプルの `#44` はクリック時に行へ移動できる `#L44` に補正する。ラベルのコード片はソースから取得し、サンプルの説明文を推測で補わない。`selector path` は入力された selector を示し、Map で省いた `div` から再生成しない。`exec command` は対象と指定済みオプションを含め、再実行できる形にする。

実際の条件や未解決箇所がある場合に確定した通信として見せない。選んだ経路が API までつながらなければ、その位置で停止したことを短く示す。この例外は、根拠のない23行を作らないために必要なものとする。

## 3. 23行を作るための内部設計

### 3.1 現行モデルの不足

`operations[*].edgeIds` は因果順の一本道ではない。`data-id=nameField` の報告には18イベントがあり、`keydown.enter` の操作には後続イベントから伝播した辺も含まれる。HTTP 辺の有無や配列順だけでは、`textChanged`、`experimentNameChanged` を通る経路を決められない。全操作を並べる方法でもサンプルの経路にならない。

### 3.2 追跡順の作成

短い表示では、対象入力の `keydown.tab`／`keydown.enter` から、同じ所有者の emit と次の output 購読を照合して後続 operation をたどる。`--event` 指定時も後続イベントを解析できるよう、simple 用の組み立てでは全イベントを保持し、表示の起点だけを指定イベントに絞る。伝播済みの HTTP 辺が最初の operation にあっても、後続の購読を先にたどる。

この選択処理は `src/render/simple.ts` の `operationChains` にまとめる。既存の詳細 Markdown と JSON は従来どおり `WiringReport` から生成し、モデルのスキーマは変えない。呼び出し式と使用位置のラベルは report の根拠位置でソースを読み、無ければモデルのラベルに戻す。

### 3.3 表示経路の選択

`path.occurrenceIds` を root 側から読み、サンプルの役割を持つ位置を残す。bootstrap、route、`router-outlet`、外部部品の使用位置、コンポーネント、`@if`、`form`、対象 `input` が該当する。単なるラッパーの `div` は省く。ただし表示条件や挿入位置の根拠になる要素をタグ名だけで削らない。

データ部は追跡順の主経路から、サンプルの `keydown`、emit、購読、dispatch、Effect、サービス呼び出し、POST の節目を選ぶ。別イベントの HTTP や背景入力を混ぜない。`keydown.tab` と `keydown.enter` は、同じ後続経路と条件を持つことを確認してから、サンプルどおり1行にまとめる。

### 3.4 種別と関係

種別 `:B`／`:R`／`:O`／`:L`／`:h`／`:@`／`:C`／`:D`／`:E`／`:S`／`:?` はサンプルの凡例に従う。dispatch は `.ts` にあっても `:D` を優先する。外部部品 `:L` は定義元を確認できた場合に使い、使用位置が `.html` というだけで決めない。

関係 `:V` は表示配置、`:C` は実際の表示制御フロー、`:D` はデータ受け渡し、`:A` は API 通信、`:-` は関係なし。ルート有効化の条件があるだけで全てを `:C` にしない。サンプル18行目の `[visible]` は外部部品の実装を確認できない限り表示条件と断定できないため、根拠に従って記号を決める。サンプルとの差分が生じた場合は受け入れ確認で理由を示す。

## 4. `--belowData`

選んだ追跡経路の最初の `:D` 行（サンプルでは09行目の `keydown`）から末尾までを出す。上の表示配置行を省き、残る行の番号を振り直す。`input-binding` など背景の別の `:D` を境目にしない。凡例と `exec command` は残す。`selector path` は表示経路を省く指定なので出さない。

## 5. 変更対象

- `src/assemble/report.ts`: simple で `--event` を指定しても後続イベントを解析対象に含める。詳細 Markdown / JSON の解析範囲は変えない。
- `src/render/simple.ts`: サンプルの行を作る新しい renderer。
- `src/render/index.ts`、`src/assemble/backend.ts`: 既定 simple、`--detail`、`--json` の振り分け。
- `src/cli/arguments.ts`、`src/cli/run.ts`: `--detail` と `--belowData` の解析とヘルプ。
- `README.md`、`x-structure.md`: 既定出力とオプションの説明。既存の受け入れ資料は、従来 Markdown を再生成するコマンドだけ `--detail` を足す。

## 6. 検証と受け入れ

1. `data-id=nameField`／`--project stackup` で、サンプルの23ステップを順に突き合わせる。各行のファイル、行番号、コード片、因果関係を確認する。サンプルと違う行は理由を記録し、推測で合わせない。
2. 途中の単なる `div`、無関係なイベント、背景入力、別の API が短い Map に混ざらないことを確認する。
3. `--detail` の本文が従来と一致し、`--json` の表示形式が変わらないことを確認する。
4. `--belowData` でサンプル09行目以降だけが残ること、リンクが開くこと、再実行コマンドが使えることを確認する。
5. `npm run build`、`npm test`、`npm run check:dist` を通す。既知の `check:contracts` 失敗件数を増やさない。

23行を出せない根拠不足があれば、その位置と原因を報告する。サンプルの見た目を優先して未接続のコードをつながない。
